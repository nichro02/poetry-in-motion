<h1 id="postTitle"><%= post.title %></h1>
<h3 id="author"><%= post.user.name %></h3>
<p id="date">Written on <%= moment(post.createdAt).format('MMMM Do, YYYY') %></p>

<img src="<%= post.image %>">
<p id="content"><%= post.content %></p>
<div id="comment">
    <h3>Comments</h3>
    <% if(post.comments.length > 0) { %>
        <% post.comments.forEach(comment => { %>
            <p><%= comment.dataValues.content %></p>
            <p><%= comment.dataValues.name %></p>
            <p><%= moment(comment.dataValues.createdAt).format('MMMM Do, YYYY') %></p>
    <% }) %>
    <% } else { %>
        <p>No comments yet</p>
    <% } %>
</div>
<h4>Add a Comment</h4>
<form action="/comments" method="POST">  
    <div>
        <label for="content">Comment</label>
        <input type="text" id="content" name="content" required>
    </div>

    <div>
        <input type="hidden" id="postId" name="postId" value="<%=post.id%>" required>
    </div>

    <div>
        <input type="hidden" id="userId" name="userId" value="<%=post.user.id%>" required>
    </div>

  <input type="submit" value="Add Comment">
</form>

<form method="POST" action="/posts/<%=post.id%>/?_method=DELETE">
    <input type="submit" value="Delete Post">
</form>

<form method="GET" action="/posts/update/<%=post.id%>/?_method=PUT">
    <input type="submit" value="Update Post">
</form>

<a href="/posts/index">&larr; Back to Index</a>

<footer>
    <p>
        Photo by <%=post.photographerName%> on <a href="https://unsplash.com/?utm_source=Poetry_In_Motion&utm_medium=referral">Unsplash</a>
    </p>
</footer>